"""
Deleting Tuples: since tuples are immutable, it is impossible to delete the element within it but that doesnt mean you cannot delete the tuple entirely. we use the del keyword when deleting tuples

SYNTAX: del(a)
where a variable_name of tuple to be deleted
"""

# Example1: 
tuple_1 = 1,2,3,4,5
print (tuple_1)  # This outputs the values ot tuple_1

del(tuple_1)     # This deletes tuple_1

