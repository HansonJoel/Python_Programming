""" There is no point of creating a list, if you can't access individual elements in that list. To understand how we access element within list, lets do an example"""

mix = [1,6,'simplilern', 'get', 'cerified']
print (mix[3])


# we can also access element in list from the backward position

print(mix[-1])  #This will output certified
print(mix[-2])  # This will output get


"""Notice, when counting index in list from forward position (left to right), the first index start counting from 0,1,2 ...etc but when you are counting from backward position (from right to left), the index begins with -1,-2,-3...etc
